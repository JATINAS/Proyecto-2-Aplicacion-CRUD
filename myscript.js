ciclo = 0;
function registro() {
    const abc = quehacer
    const tarea = {titulo: quehacer, descripcion: detalles};
    document.getElementById("demo").innerHTML = tarea.titulo.value;
    document.getElementById("demo1").innerHTML = tarea.descripcion.value;
    /*var dato = document.getElementById("homework");
    document.getElementById("demo").innerHTML = dato.options[dato.selectedIndex].text;*/
    ciclo += 1;
    
}

if (ciclo > 0) {
    function editar() {
        delete innerHTML.quehacer;
        delete Text.quehacer;
    }
}